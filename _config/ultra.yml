
remote_theme: ronv/ultra

livereload: true
baseurl: /History

defaults:
  - scope:
      path: "" # an empty string means all files
    values:
      layout: "default"

  - scope:
      path: "img"
    values:
      image: true

exclude:
  - Gemfile
  - Gemfile.lock
